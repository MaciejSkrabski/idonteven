<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/info_layout"
    android:orientation="vertical">
 <!--   <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/btn_close"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:src="@drawable/ic_close_black_24dp"
        android:padding="5dp"/>-->

<TextView
    android:textStyle="bold"
    android:id="@+id/desc"
    android:textSize="20dp"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintTop_toTopOf="parent"
    android:text="App description"
    android:paddingTop="10dp"
    android:paddingLeft="10dp"
    android:paddingBottom="5dp"
    />
    <TextView
        android:paddingBottom="10dp"
        android:paddingLeft="5dp"
        android:paddingRight="5dp"
        android:gravity="start"
        android:textSize="16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/desc"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:text="The application is used to classify words read by the program from a photo, which it downloads from the gallery or can be made using a camera built into the phone. Classifies into one of the three fonts we have selected: Lato-Regular, LiberationSans-Regular, LiberationSerif-Regular. As the classification data, it uses the brightness of the pixels from the image, which are saved in the form of a numpy array. The classification of vectors is done by the SVM method. SVM (Support Vector Machine) - an abstract concept of a machine that works as a classifier and whose science aims to determine the separation hyperplane with a maximum margin."
        />

</androidx.constraintlayout.widget.ConstraintLayout>